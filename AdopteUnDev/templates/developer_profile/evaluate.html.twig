{% extends 'base.html.twig' %}

{% block title %}Evaluate Developer{% endblock %}

{% block body %}
    <h1>Evaluate Developer: {{ developer.firstName }} {{ developer.lastName }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.rating) }}
        <button class="btn btn-primary">Submit Evaluation</button>
    {{ form_end(form) }}
{% endblock %}
