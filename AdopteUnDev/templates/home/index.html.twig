{% extends 'base.html.twig' %}

{% block title %}Bienvenue{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <h1 class="display-4 mb-4">Bienvenue sur AdopteUnDev</h1>
            <p class="lead mb-5">
                AdopteUnDev est une plateforme innovante qui met en relation des entreprises et des développeurs. 
                Les entreprises peuvent consulter des profils qualifiés, tandis que les développeurs ont l’opportunité 
                de présenter leurs compétences et de trouver des missions adaptées à leurs attentes.
            </p>

            {% if not app.user %}
                <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg me-3">Se connecter</a>
                <a href="{{ path('app_register') }}" class="btn btn-secondary btn-lg">Créer un compte</a>
            {% else %}
                <p class="mb-4">Vous êtes connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>.</p>
                <a href="{{ path('app_logout') }}" class="btn btn-danger btn-lg">Se déconnecter</a>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
